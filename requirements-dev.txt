-r requirements.txt
# Python 3.8 is the Windows 8 x86 compatibility target. pytest 8.4+ and some
# newer coverage stacks have moved their minimum Python version forward, so pin
# the legacy CI lane to versions that still support 3.8.
pytest>=8.0,<8.4; python_version < "3.9"
pytest>=8.0,<9.0; python_version >= "3.9"
pytest-cov>=5.0,<6.0; python_version < "3.9"
pytest-cov>=5.0,<7.0; python_version >= "3.9"
